public class Solution {
    // you need treat n as an unsigned value
    public int reverseBits(int n) {
        String s=String.valueOf(n);
        int k=Integer.reverse(n);
        return k;
        
    }
}